extends layouts/main
include mixins/movieBlock

block content
    .movies__movie
        if movies.length ===0
            h5  No Movies Found
        each item in movies
            +movieBlock({
                id: item.id,
                title: item.title,
            })
