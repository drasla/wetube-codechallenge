const HOME = "/";
const ABOUT = "/about";
const CONTACT = "/contact";
const LOGIN = "/login";
const PROTECTED = "/protected";

const routes = {
    home: HOME,
    about: ABOUT,
    contact: CONTACT,
    login: LOGIN,
    protected: PROTECTED
};

export default routes;
