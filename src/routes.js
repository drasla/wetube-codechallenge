const HOME = "/";
const ABOUTUS = "/about-us";
const CONTACT = "/contact";
const PROTECTED = "/protected";

const routes = {
    home: HOME,
    aboutus: ABOUTUS,
    contact: CONTACT,
    protected: PROTECTED
}

export default routes;