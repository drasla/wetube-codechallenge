const HOME = "/";
const ABOUT = "/about";
const CONTACT = "/contact";
const LOGIN = "/login";

const routes = {
    home: HOME,
    about: ABOUT,
    contact: CONTACT,
    login: LOGIN
};

export default routes;
